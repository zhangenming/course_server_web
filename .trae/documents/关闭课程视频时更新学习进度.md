## 现状
- 课程进度更新逻辑在 `recordCourseProgress` 中实现，调用位置：
  - 课程播放结束事件：`src/App.vue:322–327`
  - 列表播放器结束事件：`src/App.vue:123–136`
- 关闭课程视频逻辑：`closeVideo` 于 `src/App.vue:329–338`，当前未确保在关闭时稳定执行并等待进度更新接口（您希望 `L320–324` 的 PATCH 被调用）。
- 关闭入口：
  - 课程模态关闭按钮：`src/App.vue:864–871`
  - 点击遮罩关闭：`src/App.vue:860–867` 的 `@click.self="closeVideo"`

## 目标
- 在用户“关闭课程视频”时，必定调用并完成 `PATCH api/v1/courses/{id}/progress`（`/src/App.vue#L320-324`）。
- 避免重复提交或竞态导致的失败；在失败时保持页面流程不中断。

## 修改方案
1. 在 `src/App.vue:329–338` 的 `closeVideo` 中，插入并优先执行：
   - `await recordCourseProgress()`（将 `try { recordCourseProgress().catch(() => {}) }` 改为显式 `await`，并放在切换 overlay 之前），确保提交完成后再切换到问卷主题。
2. 轻量去重保护（可选，若担心与 `onVideoEnded` 重复）：
   - 增加 `const postedCourseIds = new Set<number>()`；在 `recordCourseProgress` 内部判断 `if (postedCourseIds.has(courseId)) return`，成功后 `postedCourseIds.add(courseId)`。
   - 保持幂等，避免结束与关闭同时触发的双 PATCH。
3. 错误处理：
   - `recordCourseProgress` 的 `catch` 仅 `console.warn`；保持用户流程连续。
   - 如需可视化提示，可在 `catch` 中调用 `notifyListError('记录课程进度失败')`（默认不提示，防止打扰）。
4. 兼容全屏：
   - 维持已加的 `exitFullscreen` 存在性检查，防止 `Document not active` 异常干扰进度提交。

## 代码定位
- 插入/调整位置：`src/App.vue:329–338`（`closeVideo`）。
- 进度提交实现：`src/App.vue:312–318` 定义、`src/App.vue:320–324` 执行 PATCH。

## 验证
- 手动流程：
  - 播放课程视频 → 点击关闭按钮或遮罩 → 观察网络面板出现 `PATCH api/v1/courses/{id}/progress` → 状态 2xx。
  - 再次尝试通过“播放结束”关闭，确认不会重复提交（若启用去重）。
- 构建检查：`pnpm build` 保持通过。

## 影响面
- 仅影响课程视频关闭流程；对播放结束事件与列表播放器流程无破坏。

请确认以上方案后，我将按此实施并完成验证。